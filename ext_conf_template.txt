# cat=basic; type=text; label=Default screen width; When a a screen width cannot be determined from an existing cookie or useragent, this value will be used as the default.
defaultScreenWidth = 1024

# cat=basic; type=boolean; label=Allow content elements to be filtered by profile; All content elements to be restricted to specific profiles by adding a selector to each content element.
filterContentElementsByProfile = 1